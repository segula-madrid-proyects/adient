{
  "name": "adient",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n back,front -c blue,green \"pnpm --filter adient-server dev\" \"pnpm --filter adient-client dev\"",
    "dev:back": "pnpm --filter adient-server dev",
    "dev:front": "pnpm --filter adient-client dev",
    "build": "pnpm --filter adient-server build && pnpm --filter adient-client build",
    "start": "pnpm --filter adient-server start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "packageManager": "pnpm@10.8.1"
}
